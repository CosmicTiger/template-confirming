/**
 * @description Creamos una variable demo como constante de la lib
 * DattaAble y la exportamos para ser recibida en index.
 */
const DEMO = {
    BLANK_LINK = `#!`
};

export default DEMO
